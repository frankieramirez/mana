# Behavioral acceptance

Grade the artifacts and operation log, not exact headings or wording.

1. Uses #137 as existing planning; proposes its clear association in a draft and does not create or recommend a duplicate map. Does not claim the draft link was saved or invent a frontier ticket.
2. Accounts for shipped baseline, and gives every destination requirement a milestone or explicit baseline evidence, including both installation environments.
3. Explains sequencing and milestone value. Broad release scope has meaningful workstreams or a justified split. Completion criteria identify observable proof.
4. Distinguishes reported delivery from verified outcome. Closed partial work cannot complete a broader milestone. Explicit confirmation is identified as such.
5. Renders a useful issue and concise conversational assessment without visible work-kind metadata, empty Maps/Efforts fields, or a forced administrative table. Names the unresolved release-placement decision for web components.
6. Honors draft/read-only scope. Compatibility cases resolve pointer, label, and legacy marker correctly. Failed reads or guarded writes cannot become invented success.

Record pass/fail with artifact evidence for each applicable criterion. Mark unexecuted cases as not run. Do not count a static instruction review as a behavioral run.
