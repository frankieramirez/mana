{"id": "pr-faithful", "source": "Write the PR description for this change. Return only the description, with no notes or caveats afterwards.\n\nThe auth middleware now caches the JWKS response for 10 minutes instead of fetching it on every request. I added a test that asserts the cache is bypassed when the key ID is unknown. p99 on /api/me went from 340ms to 45ms in staging.", "output": "Cache JWKS responses in the auth middleware for 10 minutes. Unknown key IDs bypass the cache, covered by a new test. In staging, /api/me p99 fell from 340ms to 45ms.", "expected": {"unsupported_claim": false, "meaning_lost": false}, "label_note": ""}
{"id": "pr-invented-purpose", "source": "Write the PR description for this change. Return only the description, with no notes or caveats afterwards.\n\nThe auth middleware now caches the JWKS response for 10 minutes instead of fetching it on every request. I added a test that asserts the cache is bypassed when the key ID is unknown. p99 on /api/me went from 340ms to 45ms in staging.", "output": "Cache JWKS responses for 10 minutes to keep identity-provider outages from interrupting logins. A new test checks cache bypass for unknown key IDs. In staging, /api/me p99 fell from 340ms to 45ms.", "expected": {"unsupported_claim": true, "meaning_lost": false}, "label_note": "The source gives no outage rationale; this wording also avoids the existing invention regex."}
{"id": "pr-wrong-environment", "source": "Write the PR description for this change. Return only the description, with no notes or caveats afterwards.\n\nThe auth middleware now caches the JWKS response for 10 minutes instead of fetching it on every request. I added a test that asserts the cache is bypassed when the key ID is unknown. p99 on /api/me went from 340ms to 45ms in staging.", "output": "Cache JWKS responses for 10 minutes. A new test checks cache bypass for unknown key IDs. In production, /api/me p99 fell from 340ms to 45ms.", "expected": {"unsupported_claim": true, "meaning_lost": true}, "label_note": "The source says staging, not production."}
{"id": "pr-missing-condition", "source": "Write the PR description for this change. Return only the description, with no notes or caveats afterwards.\n\nThe auth middleware now caches the JWKS response for 10 minutes instead of fetching it on every request. I added a test that asserts the cache is bypassed when the key ID is unknown. p99 on /api/me went from 340ms to 45ms in staging.", "output": "Cache JWKS responses for 10 minutes. In staging, /api/me p99 fell from 340ms to 45ms.", "expected": {"unsupported_claim": false, "meaning_lost": true}, "label_note": "Drops the unknown-key bypass and its test."}
{"id": "rotation-supported", "source": "Rewrite concisely without changing the facts: Unknown key IDs bypass the cache so rotated keys can be fetched. The cache lifetime is 10 minutes.", "output": "Cache keys for 10 minutes; bypass the cache for unknown IDs to fetch rotated keys.", "expected": {"unsupported_claim": false, "meaning_lost": false}, "label_note": "Rotation is explicitly supported here, although the existing PR invention regex rejects that word."}
{"id": "rotation-invented", "source": "Rewrite concisely without changing the facts: Unknown key IDs bypass the cache so rotated keys can be fetched. The cache lifetime is 10 minutes.", "output": "Cache keys for 10 minutes; bypass the cache for unknown IDs to fetch rotated keys. This also prevents replay attacks.", "expected": {"unsupported_claim": true, "meaning_lost": false}, "label_note": ""}
{"id": "uncertainty-kept", "source": "Rewrite concisely: The migration may reduce query latency. We have not measured production performance yet.", "output": "The migration may reduce query latency; production performance is unmeasured.", "expected": {"unsupported_claim": false, "meaning_lost": false}, "label_note": ""}
{"id": "uncertainty-lost", "source": "Rewrite concisely: The migration may reduce query latency. We have not measured production performance yet.", "output": "The migration reduces query latency. Production performance is unmeasured.", "expected": {"unsupported_claim": true, "meaning_lost": true}, "label_note": ""}
{"id": "conditions-kept", "source": "Rewrite concisely: The worker retries only timeout errors. Permission failures stop immediately.", "output": "The worker retries timeouts and stops immediately on permission failures.", "expected": {"unsupported_claim": false, "meaning_lost": false}, "label_note": ""}
{"id": "conditions-broadened", "source": "Rewrite concisely: The worker retries only timeout errors. Permission failures stop immediately.", "output": "The worker retries errors and stops immediately on permission failures.", "expected": {"unsupported_claim": true, "meaning_lost": true}, "label_note": ""}
{"id": "code-kept", "source": "Rewrite the prose; leave code unchanged and do not derive new prose facts from it.\nThe client retries failed requests.\n```python\nsleep(2 ** attempt)\n```", "output": "The client retries failed requests.\n```python\nsleep(2 ** attempt)\n```", "expected": {"unsupported_claim": false, "meaning_lost": false}, "label_note": ""}
{"id": "code-derived-mechanics", "source": "Rewrite the prose; leave code unchanged and do not derive new prose facts from it.\nThe client retries failed requests.\n```python\nsleep(2 ** attempt)\n```", "output": "The client retries failed requests, doubling the wait each time.\n```python\nsleep(2 ** attempt)\n```", "expected": {"unsupported_claim": true, "meaning_lost": false}, "label_note": "Imports a mechanism from exempt code into the prose."}
